---
name: load-check
description: "Нагрузка роутера сейчас и сравнение с нормой по часам: CPU, память, активные соединения, load average, скорость WAN; «всё ли в норме», «тормозит»."
---

# Skill: Нагрузка роутера и сравнение с нормой

Используй, когда владелец спрашивает про нагрузку, производительность,
«всё ли в норме», тормозит ли роутер, CPU/память/соединения/скорость.

## Данные

1. `/opt/etc/observer/state/metrics.now` — ТЕКУЩИЕ метрики (обновляются каждые 5 минут):
   - cpu_pct — загрузка CPU, %
   - mem_pct — занятая RAM, %
   - conn_active — активные соединения (NAT-сессии); базовая метрика роутера
   - loadavg1 — load average ×100 (напр. 118 = 1.18)
   - wan_kbps — скорость на аплинке, КиБ/с
   В шапке файла — «слот часа: N» (час суток 0-23).

2. `/opt/etc/observer/state/baseline.tsv` — НОРМА по часам суток. Строки:
   METRIC<TAB>SLOT<TAB>N<TAB>MEAN<TAB>SPREAD
   (METRIC: cpu mem conn load wan; SLOT: час 0-23; MEAN — типичное значение,
   SPREAD — типичный разброс σ, N — сколько СУТОК норма этого часа набиралась).
   Норма каждого часа появляется после первого его прохождения (≈сутки),
   разброс уже реальный (из ~12 замеров за час). N=1 — норма есть, но ещё
   не подтверждена вторыми сутками; N>=2 — уверенная.

## Как отвечать

- Возьми слот часа из шапки metrics.now, найди в baseline.tsv строки
  этого слота и сопоставь каждую текущую метрику с её нормой.
- Формулируй как «X сейчас Y при норме ~MEAN±SPREAD для этого часа»:
  - в пределах ±SPREAD (или чуть больше) — НОРМА;
  - сильно выше/ниже (в разы или > 3-4×SPREAD) — отклонение, укажи явно
    и предположи причину (много соединений → торрент/скан; высокий CPU
    → нагрузка/обновление; wan≈0 в активный час → возможен обрыв).
- Если для слота нет строки — этот час ещё ни разу не набирался (первые
  сутки), честно скажи: «норма для этого часа ещё не собрана, текущие
  значения такие-то». Если N=1 — норма предварительная (одни сутки).
- Ключевой смысл: абсолютное число само по себе мало значит; ценно
  ОТКЛОНЕНИЕ от типичного для этого времени.

## Ограничения

- Только читаю метрики и норму, ничего не запускаю и не меняю.
- Норма привязана к часу суток (нагрузка ночью и вечером разная).
