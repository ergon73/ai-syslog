# Уровень 1: "известный шум". Строки, совпавшие с шаблоном (ERE, без учёта
# регистра), НЕ будят LLM в реальном времени, но копятся и попадают
# в утренний дайджест. Применяется к агрегированной строке "PROGRAM<TAB>MSG".

# DoH-резолверы: отдельные неудачные попытки — шум; тренд виден в дайджесте
curl error message: Failed to connect

# WireGuard: штормы рукопожатий при недоступном пире (10-20 тыс. строк/сутки)
wireguard: .* handshake for peer .* (retrying|giving up)
wireguard: .* peer .* \((created|destroyed)\)?

# Переподключения WireGuard-пиров (ndm): сопровождают каждый флап VPN,
# тренд виден в дайджесте; реалтайм-алерт на каждый флап = спам
Wireguard::Interface: .* resolved peer
EndpointTracker: "Wireguard
Network::Interface::(Ip|Ip6|Base): "Wireguard
Core::System::StartupConfig: saving \(coala

# DHCP-продления известных клиентов — шум. НО STATE_INIT (первый запрос
# от нового/вернувшегося устройства) НЕ глушим: это сигнал «новое устройство».
DHCP(REQUEST|DISCOVER|INFORM) received \(STATE_(RENEWING|REBINDING|BOUND)\)
DHCP(OFFER|ACK|NAK)

# Wi-Fi ассоциации клиентов и возрастные отключения
had (dis)?associated
aged-out and disassociated

# Деградация DoH/DoT и её обслуживание: тренд для дайджеста
# (важно: "MIC differs" — попытка с неверным паролем Wi-Fi — сюда НЕ входит)
Dns::Secure::Tools: fallback to recursive DNS
Dns::Manager: updating DNS-over-(TLS|HTTPS)

# Продление собственной DHCP-аренды на аплинке
received ACK for IP

# DHCPINFORM-обмен (Windows-клиенты уточняют параметры) — шум
DHCPINFORM received
sending INFORM to

# Сброс conntrack при смене состояния интерфейсов (следствие флапов VPN)
Netfilter::Util::Conntrack: flushed

# UPnP-проброс портов через VPN-туннель (Policy0 -> WireguardN): рутинная
# работа P2P/torrent-приложения, привязанного к VPN. Сотни строк/сутки,
# бот алертил на каждую пачку. Тренд остаётся виден в дайджесте; реальный
# сигнал (новый target-IP) при желании смотрится там. Прочие UPnP-события
# (не redirect/forward rule added) в этот фильтр НЕ входят.
UPnP::Service: .* (redirect|forward) rule added

# dropbear: успешные логины с других хостов увидит дайджест;
# неудачные попытки (bad password, exit before auth) сюда НЕ входят — они важные
Child connection from
Pubkey auth succeeded
Password auth succeeded
Exit \(root\) from <
