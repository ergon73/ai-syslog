# Переопределение имён устройств по MAC или IP.
# Имена из DHCP подхватываются автоматически из логов — здесь только то,
# что DHCP знать не может (роль устройства, статичные адреса без DHCP).
# Формат:  TOKEN = имя        (TOKEN — MAC или IP, регистр не важен)
#
# Скопируйте в hosts.txt и заполните под свою сеть. hosts.txt в .gitignore.

# 192.168.6.70 = ноутбук-коллектор ai-syslog (этот сервер)
# ec:8e:77:bc:65:65 = рабочий ноутбук
# 192.168.6.1 = роутер Keenetic (шлюз)
